# {{ ansible_managed }}

{
	storage file_system {
		root "/data/"
	}
}

{{ common_name }} {
	root * /static

	file_server
}

r.{{ common_name }} {
	root * /static/.r

	file_server
}

import /config/conf.d/*
